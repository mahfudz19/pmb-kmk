document.addEventListener("DOMContentLoaded",()=>{initSpaNavigation();const config=window.mazuConfig?.spa||{};if(config.prefetch&&window.SpaPrefetcher){const saveData=navigator.connection?.saveData;if(saveData){console.log("Mazu SPA: Prefetching disabled due to Data Saver mode.");return;}window.mazuPrefetcher=new SpaPrefetcher({maxConcurrent:config.prefetch_limit||5,});window.mazuPrefetcher.init();console.log("Mazu SPA: Smart Prefetcher active",{limit:config.prefetch_limit,});}});function initSpaNavigation(){if("scrollRestoration" in history){history.scrollRestoration="manual";}document.body.addEventListener("click",(e)=>{const link=e.target.closest("a");if(!link||!link.hasAttribute("data-spa")){return;}const href=link.getAttribute("href");if(!href||href.trim()===""||href.startsWith("#")||href.startsWith("javascript:")||link.target==="_blank"||link.hasAttribute("download")){return;}e.preventDefault();const url=link.href;let shouldScroll=true;if(link.hasAttribute("data-spa-no-scroll")||link.getAttribute("data-spa-scroll")==="false"){shouldScroll=false;}const options={scroll:shouldScroll,method:"GET",};navigateTo(url,options);});document.body.addEventListener("submit",(e)=>{const form=e.target.closest("form[data-spa]");if(!form)return;e.preventDefault();const action=form.getAttribute("action")||window.location.href;const method=(form.getAttribute("method")||"GET").toUpperCase();const formData=new FormData(form);navigateTo(action,{method,body:method==="GET"?null:formData,});});window.addEventListener("popstate",(e)=>{if(e.state&&e.state.url){const restoreScrollPos=e.state.scrollPosition||null;loadContent(e.state.url,{...e.state.options,pushState:false,restoreScroll:restoreScrollPos,});}else{window.location.reload();}});}async function navigateTo(url,options={}){saveCurrentScrollPosition();if(!options.method||options.method==="GET"){if(options.replace){history.replaceState({url:url,options:options},"",url);}else{history.pushState({url:url,options:options},"",url);}}await loadContent(url,{...options,pushState:options.method==="GET"});}const spaCache=new Map();const CACHE_TTL=5*60*1000;let activeNavigationController=null;function clearSpaCache(){spaCache.clear();}const spaConfig={requestInterceptors:[],};function registerRequestInterceptor(callback){if(typeof callback==="function"){spaConfig.requestInterceptors.push(callback);}}function getAuthConfig(){return window.mazuConfig?.auth||{};}function getStorage(storageName){if(storageName==="sessionStorage")return sessionStorage;if(storageName==="localStorage")return localStorage;return null;}function getCookieValue(name){const value=`; ${document.cookie}`;const parts=value.split(`; ${name}=`);if(parts.length===2)return parts.pop().split(";").shift()||null;return null;}function deleteCookie(name){document.cookie=`${name}=; Max-Age=0; path=/`;}function readAuthToken(){const auth=getAuthConfig();const storageName=auth.token_storage||"cookie";const tokenKey=auth.token_key||"token";const cookieName=auth.token_cookie||tokenKey;if(storageName==="cookie"){return getCookieValue(cookieName);}const storage=getStorage(storageName);if(!storage)return null;try{return storage.getItem(tokenKey);}catch(e){return null;}}function clearAuthStorage(){const auth=getAuthConfig();const storageName=auth.token_storage||"cookie";const tokenKey=auth.token_key||"token";const userKey=auth.user_key||"user";const cookieName=auth.token_cookie||tokenKey;if(storageName==="cookie"){deleteCookie(cookieName);return;}const storage=getStorage(storageName);if(!storage)return;try{storage.removeItem(tokenKey);if(userKey){storage.removeItem(userKey);}}catch(e){}}const authConfig=getAuthConfig();if(authConfig&&authConfig.mode==="token"&&authConfig.auto_attach!==false){registerRequestInterceptor((headers)=>{const token=readAuthToken();if(token){const headerName=authConfig.token_header||"Authorization";const prefix=authConfig.token_prefix||"Bearer";headers[headerName]=prefix?`${prefix} ${token}`:token;}});}async function loadContent(url,options={}){const{pushState=true,scroll=true,ignoreCache=false,method="GET",body=null,}=typeof options==="boolean"?{pushState:options}:options;if(activeNavigationController){activeNavigationController.abort();}const controller=new AbortController();activeNavigationController=controller;const{signal}=controller;startProgressBar();const targetContainer=getDeepestLayoutContainer();const loadingContainer=targetContainer||document.getElementById("app-content");const cached=spaCache.get(url);const now=Date.now();let isServedFromCache=false;if(method==="GET"&&!ignoreCache&&cached&&now-cached.timestamp<CACHE_TTL){const targetExists=cached.data.meta&&cached.data.meta.layout?document.querySelector(`[data-layout="${cached.data.meta.layout}"]`):true;if(targetExists){renderSpaResponse(cached.data,url,options);isServedFromCache=true;}else{}}let originalHTML=null;const isAuthRoute=/^\/(login|register|password|verify-otp|otp-sent|logout|resend-otp|resend-verification)/.test(new URL(url,window.location.origin).pathname);if(isAuthRoute){if(!isServedFromCache&&loadingContainer){loadingContainer.style.opacity="0.5";loadingContainer.style.pointerEvents="none";}}else{if(!isServedFromCache&&loadingContainer){originalHTML=loadingContainer.innerHTML;loadingContainer.innerHTML=`
        <div class="space-y-6 animate-pulse p-6 md:p-8">
          <div class="h-20 bg-slate-100 rounded-3xl w-full"></div>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            <div class="h-28 bg-slate-100 rounded-3xl"></div>
            <div class="h-28 bg-slate-100 rounded-3xl"></div>
            <div class="h-28 bg-slate-100 rounded-3xl"></div>
          </div>
          <div class="h-64 bg-slate-100 rounded-3xl w-full"></div>
        </div>
      `;loadingContainer.style.pointerEvents="none";}}const targetLayout=targetContainer?targetContainer.getAttribute("data-layout"):"layout.php";const activeLayouts=[];document.querySelectorAll("[data-layout]").forEach((el)=>{activeLayouts.push(el.getAttribute("data-layout"));});const headers={"X-SPA-REQUEST":"true","X-SPA-TARGET-LAYOUT":targetLayout,"X-SPA-LAYOUTS":JSON.stringify(activeLayouts),};if(method!=="GET"){const csrfToken=document.querySelector('meta[name="csrf-token"]')?.getAttribute("content");if(csrfToken){headers["X-CSRF-TOKEN"]=csrfToken;}}spaConfig.requestInterceptors.forEach((interceptor)=>{try{interceptor(headers,url);}catch(e){console.error("SPA Interceptor Error:",e);}});try{const response=await fetch(url,{method:method,headers:headers,body:body,signal:signal,});if(response.status===304){if(cached)cached.timestamp=Date.now();return;}if(!response.ok){throw new Error(`HTTP error! status: ${response.status}`);}const contentType=response.headers.get("content-type");if(contentType&&contentType.includes("application/json")){const data=await response.json();if(data.action==="logout"){const auth=getAuthConfig();if(auth.auto_logout!==false){clearAuthStorage();}const target=data.redirect||auth.redirect_login||"/login";if(data.force_reload){window.location.href=target;}else{navigateTo(target,{replace:true});}return;}if(data.redirect){if(data.new_tab){window.open(data.redirect,"_blank");return;}if(data.force_reload){window.location.href=data.redirect;}else{navigateTo(data.redirect,{replace:true});}return;}spaCache.set(url,{data:data,timestamp:Date.now(),});renderSpaResponse(data,url,options);}else{window.location.href=url;}}catch(error){if(error.name==="AbortError"){console.log("SPA Navigation aborted:",url);return;}console.error("SPA Navigation Error:",error);if(!isServedFromCache){if(loadingContainer&&originalHTML!==null){loadingContainer.innerHTML=originalHTML;}window.location.href=url;}}finally{if(activeNavigationController===controller){if(loadingContainer){loadingContainer.style.opacity="1";loadingContainer.style.pointerEvents="auto";}finishProgressBar();activeNavigationController=null;}}}function startProgressBar(){const bar=document.getElementById("global-progress-bar");const inner=document.getElementById("global-progress-bar-inner");if(!bar||!inner)return;if(window.spaProgressInterval)clearInterval(window.spaProgressInterval);bar.style.opacity=1;inner.style.width="0%";inner.style.transition="width 0.2s ease-out";let width=0;window.spaProgressInterval=setInterval(()=>{if(width<90){const remaining=90-width;const increment=Math.max(0.5,remaining/20);width+=increment;inner.style.width=`${width}%`;}},100);}function finishProgressBar(){const bar=document.getElementById("global-progress-bar");const inner=document.getElementById("global-progress-bar-inner");if(!bar||!inner)return;if(window.spaProgressInterval)clearInterval(window.spaProgressInterval);inner.style.width="100%";setTimeout(()=>{bar.style.opacity=0;setTimeout(()=>{inner.style.width="0%";},300);},500);}function renderSpaResponse(data,url,options={}){if(data.meta&&data.meta.title){document.title=data.meta.title;}if(data.meta&&data.meta.csrf_token){const csrfMeta=document.querySelector('meta[name="csrf-token"]');if(csrfMeta){csrfMeta.setAttribute("content",data.meta.csrf_token);}}if(data.meta&&data.meta.styles){handleNewStyles(data.meta.styles);}let containerToUpdate=null;if(data.meta&&data.meta.layout){const potentialLayouts=Array.isArray(data.meta.layout)?data.meta.layout:[data.meta.layout];for(const layoutId of potentialLayouts){const el=document.querySelector(`[data-layout="${layoutId}"]`);if(el){containerToUpdate=el;break;}}if(!containerToUpdate){}}if(containerToUpdate){const activeElement=document.activeElement;const isFocusInside=containerToUpdate.contains(activeElement);window.dispatchEvent(new Event("spa:before-navigate"));containerToUpdate.innerHTML=data.html;executeScripts(containerToUpdate);handleAfterNavigation(options);if(isFocusInside){containerToUpdate.setAttribute("tabindex","-1");containerToUpdate.focus({preventScroll:true});containerToUpdate.style.outline="none";}window.dispatchEvent(new Event("spa:navigated"));}else{const rootContainer=document.getElementById("app-content");if(rootContainer){const activeElement=document.activeElement;const isFocusInside=rootContainer.contains(activeElement);window.dispatchEvent(new Event("spa:before-navigate"));rootContainer.innerHTML=data.html;executeScripts(rootContainer);handleAfterNavigation(options);if(isFocusInside){rootContainer.setAttribute("tabindex","-1");rootContainer.focus({preventScroll:true});rootContainer.style.outline="none";}window.dispatchEvent(new Event("spa:navigated"));}else{window.location.href=url;}}}function saveCurrentScrollPosition(){const mainEl=document.querySelector("main");let scrollY=window.scrollY;let scrollX=window.scrollX;if(mainEl){const scrollParent=getScrollParent(mainEl);if(scrollParent&&scrollParent!==window){scrollY=scrollParent.scrollTop;scrollX=scrollParent.scrollLeft;}}const currentState=history.state||{};history.replaceState({...currentState,scrollPosition:{x:scrollX,y:scrollY}},"",);}function handleNewStyles(styles){if(!Array.isArray(styles))return;const head=document.head;const existingLinks=Array.from(head.querySelectorAll('link[rel="stylesheet"]'),).map((link)=>link.getAttribute("href"));styles.forEach((stylePath)=>{const encodedPath=encodeURIComponent(stylePath);const searchPattern=`build/assets/${stylePath}`;const exists=existingLinks.some((href)=>href.includes(searchPattern));if(!exists){const link=document.createElement("link");link.rel="stylesheet";let baseUrl=window.mazuConfig?.base_url||"/";if(!baseUrl.endsWith("/"))baseUrl+="/";link.href=`${baseUrl}build/assets/${stylePath}`;head.appendChild(link);}});}window.navigateTo=navigateTo;window.spa={push:navigateTo,refresh:()=>loadContent(window.location.href,{pushState:false,ignoreCache:true}),back:()=>window.history.back(),clearCache:clearSpaCache,onRequest:registerRequestInterceptor,};function getDeepestLayoutContainer(){const containers=document.querySelectorAll("[data-layout]");if(containers.length===0)return null;let deepest=containers[0];let maxDepth=0;containers.forEach((el)=>{let depth=0;let parent=el.parentNode;while(parent){depth++;parent=parent.parentNode;}if(depth>maxDepth){maxDepth=depth;deepest=el;}});return deepest;}function executeScripts(element){const scripts=element.querySelectorAll("script");scripts.forEach((oldScript)=>{const newScript=document.createElement("script");Array.from(oldScript.attributes).forEach((attr)=>newScript.setAttribute(attr.name,attr.value),);newScript.appendChild(document.createTextNode(oldScript.innerHTML));oldScript.parentNode.replaceChild(newScript,oldScript);});}function handleAfterNavigation(options={}){if(options.restoreScroll){const{x,y}=options.restoreScroll;const scrollOptions={top:y,left:x,behavior:"auto"};window.scrollTo(scrollOptions);const mainEl=document.querySelector("main");if(mainEl){mainEl.scrollTo(scrollOptions);const scrollParent=getScrollParent(mainEl);if(scrollParent&&scrollParent!==window){scrollParent.scrollTo(scrollOptions);}}return;}if(options.scroll!==false){const scrollOptions={top:0,left:0};window.scrollTo(scrollOptions);const mainEl=document.querySelector("main");if(mainEl){mainEl.scrollTo(scrollOptions);const scrollParent=getScrollParent(mainEl);if(scrollParent&&scrollParent!==window){scrollParent.scrollTo(scrollOptions);}}}if(window.innerWidth<1024){const sidebar=document.getElementById("sidebar");const sidebarOverlay=document.getElementById("sidebarOverlay");if(sidebar)sidebar.classList.remove("open");if(sidebarOverlay)sidebarOverlay.classList.remove("open");}}function getScrollParent(node){if(!node||node===document.body||node===document.documentElement){return window;}const style=window.getComputedStyle(node);const overflowY=style.overflowY;const isScrollable=["auto","scroll","overlay"].includes(overflowY);if(isScrollable){return node;}return getScrollParent(node.parentNode);}class SpaPrefetcher{constructor(options={}){this.maxConcurrent=options.maxConcurrent||5;this.queue=[];this.activeCount=0;this.observedUrls=new Set();this.cache=spaCache;this.observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting){const link=entry.target;const url=link.href;if(url&&!this.observedUrls.has(url)){this.observedUrls.add(url);this.addToQueue(url);this.observer.unobserve(link);}}});},{threshold:0.1},);}init(){this.observeNewLinks();window.addEventListener("spa:navigated",()=>{setTimeout(()=>this.observeNewLinks(),100);});}observeNewLinks(){const currentUrl=new URL(window.location.href);document.querySelectorAll("a[data-spa]:not([data-prefetched])").forEach((link)=>{const url=link.href;try{const targetUrl=new URL(url,window.location.origin);if(targetUrl.origin===currentUrl.origin&&targetUrl.pathname===currentUrl.pathname&&targetUrl.search===currentUrl.search){link.setAttribute("data-prefetched","true");return;}}catch(e){}if(this.cache.has(url)){link.setAttribute("data-prefetched","true");return;}this.observer.observe(link);});}addToQueue(url){if(this.cache.has(url))return;this.queue.push(url);this.processQueue();}async processQueue(){if(this.activeCount>=this.maxConcurrent||this.queue.length===0){return;}const url=this.queue.shift();this.activeCount++;try{const targetContainer=getDeepestLayoutContainer();const targetLayout=targetContainer?targetContainer.getAttribute("data-layout"):"layout.php";const activeLayouts=[];document.querySelectorAll("[data-layout]").forEach((el)=>{activeLayouts.push(el.getAttribute("data-layout"));});const headers={"X-SPA-REQUEST":"true","X-SPA-TARGET-LAYOUT":targetLayout,"X-SPA-LAYOUTS":JSON.stringify(activeLayouts),"X-PREFETCH":"true",};spaConfig.requestInterceptors.forEach((interceptor)=>{try{interceptor(headers,url);}catch(e){}});const response=await fetch(url,{headers});if(response.status===304){return;}if(response.ok){const contentType=response.headers.get("content-type");if(contentType&&contentType.includes("application/json")){const data=await response.json();this.cache.set(url,{data:data,timestamp:Date.now(),});document.querySelectorAll(`a[href="${url}"]`).forEach((el)=>el.setAttribute("data-prefetched","true"));}}}catch(error){}finally{this.activeCount--;setTimeout(()=>this.processQueue(),50);}}}window.SpaPrefetcher=SpaPrefetcher;function normalizeUrlWithBaseUrl(url,baseUrl){if(!url||url.startsWith('http://')||url.startsWith('https://')||url.startsWith('#')){return url;}if(url.startsWith(baseUrl)){return url;}if(url.startsWith('/')){return baseUrl+url;}return url;}function applyBaseUrlNormalization(){const baseUrl=window.mazuConfig?.base_url||'';let processedLinks=0;let processedForms=0;document.querySelectorAll('a[data-spa]').forEach(link=>{const href=link.getAttribute('href');if(href){const newHref=normalizeUrlWithBaseUrl(href,baseUrl);if(newHref!==href){link.setAttribute('href',newHref);processedLinks++;}}});document.querySelectorAll('form[data-spa]').forEach(form=>{const action=form.getAttribute('action');if(action){const newAction=normalizeUrlWithBaseUrl(action,baseUrl);if(newAction!==action){form.setAttribute('action',newAction);processedForms++;}}});}const observer=new MutationObserver(function(mutations){mutations.forEach(function(mutation){if(mutation.type==='childList'){setTimeout(applyBaseUrlNormalization,100);}});});observer.observe(document.body,{childList:true,subtree:true});document.addEventListener('DOMContentLoaded',applyBaseUrlNormalization);window.addEventListener('spa:navigated',applyBaseUrlNormalization);setInterval(applyBaseUrlNormalization,2000);